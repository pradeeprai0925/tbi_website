git add --all
git commit -m "<updated xxx file>"
git push -u origin main

git push origin main --force

git config --global user.email "venkatapradeeprai0925@gmail.com"
  git config --global user.name "Pradeep"